<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:Lab6_Starter.Model"
             x:DataType="model:BusinessLogic" 
             x:Class="Lab6_Starter.VisitedAirportsPage"
             Title="Airports">



    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="AddAirportBtn" IconImageSource="plus.png" Text="Add"
            Clicked="AddAirport_Clicked" />
        <ToolbarItem x:Name="LogoutBtn" IconImageSource="logout.png" Text="Logout"
            Clicked="Logout_Clicked" />
    </ContentPage.ToolbarItems>
    <ScrollView>
       <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Start">

            <!-- This line says that the ItemsSource of the CollectionView is the Airports property of the Domain object (an ObservableCollection) -->
                <CollectionView x:Name="CV" ItemsSource="{Binding VisitedAirports}" SelectionMode="Single">
                    <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="model:VisitedAirport">
                        <Grid RowDefinitions="*" ColumnDefinitions="*,2*,2*, 2*">
                            <Label Grid.Column="0" Text="{Binding Id}" VerticalTextAlignment="Center"/>
                            <Label Grid.Column="1" Text="{Binding Name}" VerticalTextAlignment="Center"/>
                            <Label Grid.Column="2" Text="{Binding DateVisited, StringFormat='{0:MM/dd/yyyy}'}" VerticalTextAlignment="Center"/>
                            <Label Grid.Column="3" Text="{Binding Rating}" VerticalTextAlignment="Center" />
                            <HorizontalStackLayout Grid.Column="4">
                            <Button MaximumWidthRequest="75" BackgroundColor="Transparent"
                                Clicked="EditAirport_Clicked" ImageSource="pencil.png" />
                            <Button MaximumWidthRequest="75" BackgroundColor="Transparent"
                                Clicked="DeleteAirport_Clicked" ImageSource="trash.png" />
                        </HorizontalStackLayout>
                        </Grid>
                    </DataTemplate>

                </CollectionView.ItemTemplate>

            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
    </ScrollView>

</ContentPage>
